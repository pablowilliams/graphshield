# ADR 002: Immutable source and mapping versions

Status: accepted. A run points to fixed input and mapping identifiers. Editing creates a new version, so old evidence remains reproducible and support staff can diagnose the configuration that actually ran.
