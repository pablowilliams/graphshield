# ADR 003: SSE with polling fallback

Status: accepted. Ordered events make progress efficient and resumable; the run resource remains authoritative and enables polling when streaming is interrupted.
